# Root premium .env.example — a production-ready, commented template
# IMPORTANT: This file contains only placeholders and examples. NEVER commit real secrets.
# Copy relevant parts into `backend/.env` and `frontend/.env.local` (or use your secret manager).

##############################
# Backend — Production / Premium
##############################

# Server
PORT=8080
NODE_ENV=production

# MongoDB (use a managed cluster for production)
MONGO_URI=mongodb+srv://<dbUser>:<dbPass>@cluster0.mongodb.net/<dbName>?retryWrites=true&w=majority
MONGO_DB_NAME=interviewprep

# Redis (caching, rate-limiting, session storage)
REDIS_URL=redis://:<redisPassword>@redis-host:6379/0

# Security / Auth
JWT_SECRET=replace_with_strong_jwt_secret
JWT_EXPIRES_IN=7d
SESSION_SECRET=replace_with_strong_session_secret
CORS_ORIGINS=https://your-frontend-domain.com,http://localhost:5173

# Rate limiting / quotas
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Cloud storage (Cloudinary or S3)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud
CLOUDINARY_API_KEY=your_cloudinary_key
CLOUDINARY_API_SECRET=your_cloudinary_secret
# Or S3
AWS_S3_BUCKET=your-s3-bucket
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_REGION=us-east-1

# AI / LLM / Gemini / OpenAI
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-pro-mix
OPENAI_API_KEY=your_openai_api_key





# Logging & monitoring
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
LOG_LEVEL=info

# Feature flags / toggles
FEATURE_DISABLE_AI=false
FEATURE_PREMIUM_ONLY=true

# Third-party integrations (OAuth, analytics) if you want
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
OAUTH_REDIRECT_URL=https://your-frontend-domain.com/auth/callback
ANALYTICS_ID=GA-XXXXXX

# Limits
MAX_UPLOAD_SIZE=10485760 # 10MB




##############################
# Dev helpers & local overrides
##############################
# For local development you can override values with a local .env file placed in each app folder.
# Example: backend/.env.local and frontend/.env.local — keep these out of git.

##############################
# Notes & Best Practices
##############################
# - Replace placeholders with real values from your secrets manager or CI environment.
# - Use short-lived credentials where possible and rotate keys regularly.
# - Store production secrets in a secure vault (AWS Secrets Manager, Azure Key Vault, HashiCorp Vault).
# - Set NODE_ENV=production and enable HTTPS in front of your backend in production.
# - Do NOT commit real `.env` files or secrets to the repository.
